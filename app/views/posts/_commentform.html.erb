<%= form_for [@post, Comment.new] do |f| %>
    <div class="input-group col-xs-12">
      <div class="col-xs-8"><%= f.text_area :comment, class: "form-control" %></div>
      <div class="col-xs-4"><%= f.submit "Comment", class: "btn" %></div>
    </div>
<% end %>
